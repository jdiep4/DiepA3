<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@color/pink_background</item>
        <item name="android:colorForeground">@color/red_foreground</item>
        <item name="android:textColorSecondaryInverse">@color/black_secondary_text</item>
        <item name="android:textColorSecondary">@color/pink_secondary_text</item>
        <item name="android:textColorPrimary">@color/red_text</item>
        <item name="android:textColorPrimaryInverse">@color/light_red_inverse_text</item>
    </style>

</resources>
